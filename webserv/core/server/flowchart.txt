WebServ(config)
	-> _socketsAddr init
	initServer()

initServer()
	-> init _sockets()
	bind()

WebServ.run()
	// loop all servers missing?
	-> listen(socket)
	struct pollfd
		.fd = _sockets;
	_fds.push_back(pollfd)
	while()
		poll
		for(all _fds)
			if (_fds[i].fd == _sockets)
				acceptconnnection() -> /!\